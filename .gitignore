.wrangler/
node_modules/

# Local-only scratch space (drafts, notes, secrets-during-rotation, etc.).
# Never committed, never deployed. See _private/README.md.
_private/
CLAUDE.md

# --- Secrets & env (added 2026-06-05; never commit) ---
.env
.env.*
!.env.example
!.env.*.example
.dev.vars
*.pem
*.key
*.p12
*.pfx
